{
  "name": "kokoapi-space",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "kokoapi.space - open-source web tools that run in the browser: PDF, images, file conversion, QR codes and encrypted sharing.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/postbuild.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "typecheck": "tsc --noEmit",
    "test": "astro check && npm run build",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:ui": "npm run build && playwright test --ui"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.49.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
